import { MainContainer } from '@components/MainContainer';
import MainSection from './components/MainSection';

export const Home = () => (
  <MainContainer>
    <MainSection />
  </MainContainer>
);
